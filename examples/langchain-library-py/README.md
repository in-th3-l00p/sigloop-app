# LangChain Library Example (Python)

This example uses `sigloop_wallet_tools.py` as the downloaded webapp library.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env.local
python main.py
```
