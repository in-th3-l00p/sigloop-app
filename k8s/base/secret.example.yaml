apiVersion: v1
kind: Secret
metadata:
  name: sigloop-secrets
  namespace: sigloop
type: Opaque
stringData:
  # Fill as needed, then apply:
  # kubectl apply -f k8s/base/secret.yaml
  PRIVY_APP_ID: ""
  SIGLOOP_API_KEY: ""
  SIGLOOP_CARD_SECRET: ""
  OPENAI_API_KEY: ""

